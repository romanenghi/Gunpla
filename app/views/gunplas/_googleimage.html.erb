<div id="googleimage">
	<%= form_tag("/googleimage", :method => "get") do %>
		<%= label_tag(:query, "Stringa di ricerca:") %>
		<%= text_field_tag(:query, @gunpla.description, :size => 40) %>
		<%= submit_tag("Search") %>
		<% end %>
</div>