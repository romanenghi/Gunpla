<div id="headergundamtype">
<% @gunplatypedescription.each_with_index do |gunplatypedescr,index|%>	
<%= link_to image_tag(gunplatypedescr['image'], :border=>10, :width => 200), "##{gunplatypedescr['title'].strip}" %>
<% end %>
</div>
<% @gunplatypedescription.each_with_index do |gunplatypedescr,index|%>
	<div id="<%=gunplatypedescr['title'].strip%>" class="gunplaype">
		<a name="<%=gunplatypedescr['title']%>"> </A>
		<%= render :partial => "gunpla", :locals => {
		:gunplas => @gunplatypelist[index] ,
		:gunplatype => gunplatypedescr
		} %>
	</div>
<% end %>

